# Daniel Mortensen portfolio site

This folder is ready for GitHub Pages. Upload every file to the root of a public GitHub repository, then enable Pages from the repository's main branch and root folder in **Settings → Pages**.

The site uses only static HTML and CSS, so it requires no build command or paid hosting.
